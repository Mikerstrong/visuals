Here's the updated prompt: Build a Streamlit app in Python using Windows NT login via getpass.getuser() to auto-authenticate—check against roles.json like {michael: submitter, alice: reviewer, bob: trainer}. Route users to role-specific pages: submitters enter idea text, optional link, pick tag from dropdown (UX, backend, etc.), save to ideas.json. Reviewers view searchable list in cards—click, vote accurate or not, earn five points in points.json. Trainers read and mark trained. Add sidebar download: generate HTML file named ideas_year_year_year_year_month_month_day_day_hour_hour_minute_minute_second_second.html—like ideas_20251217_130300.html—with all ideas in a table. Use st.download_button to let 'em grab it safely, file on disk first. Show points leaderboard too. Keep everything JSON-only, simple UI, no deletions—just adds, reviews, trains. Super clean, no extras. Make the app called tribal.py.